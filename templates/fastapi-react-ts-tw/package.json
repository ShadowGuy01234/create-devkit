{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "setup": "npm install --prefix frontend && npm run setup:backend",
    "setup:backend": "python -m pip install -r backend/requirements.txt",
    "dev": "concurrently -n frontend,backend -c cyan,magenta \"npm run dev --prefix frontend\" \"python -m uvicorn app.main:app --reload --port 5000 --app-dir backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "python -m uvicorn app.main:app --reload --port 5000 --app-dir backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
